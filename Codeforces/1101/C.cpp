#include <bits/stdc++.h>

using namespace std;

typedef long long int ll;

int main()
{
	ll T;
	cin >> T;

	while(T--)
	{
		ll n;
		cin >> n;

		ll min1=1e9, min2=1e9, max1=0, max2=0;
		for(ll i=0; i<n; i++)
		{
			cin >> l[i] >> r[i];

			if(l[i])

		}
	}


	return 0;
}