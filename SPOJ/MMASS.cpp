#include <bits/stdc++.h>

using namespace std;

typedef long long int ll;

int main()
{
	string formula;
	cin >> formula;

	stack <ll> s;

	ll len = formula.length();

	ll ans = 0;
	for(ll i=0; i<len; i++)
	{
		if(formula[i]=='(')
		{
			cnt++;
		}

		if(cnt==0)
		{

		}
	}


	return 0;
}
